.title {
    font-size: 40px;
    text-align: left;
    padding-bottom: 40px;
    @include tablets() {
        font-size: 25px;
        padding-bottom: 17px;
    }
    @include phones() {
        font-size: 22px;
    }
}